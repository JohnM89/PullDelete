

import SearchBar from '../SearchBar/SearchBar';

const Header = () => {
    return (
        <header>
            <h1>My Header</h1>
            <SearchBar />
        </header>
    );
};

export default Header;